# `a || b` and `a ?? b` are where defaults get introduced without anyone
# deciding they were wanted.
id: ts-logical-default
language: typescript
rule:
  any:
    - pattern: $A || $B
    - pattern: $A ?? $B
# Pair with tsx-logical-default.yml on any scope containing React code.
